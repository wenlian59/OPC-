# S7 实机连接测试：

> 首次部署先运行：
> - powershell -File <REPO_ROOT>\config\Sync-DataConfig.ps1
> - powershell -File <REPO_ROOT>\config\Update-PathsProps.ps1


## 0. 环境

- 宿主机已经装了 VMware Workstation 17.5.2，虚拟机里装的是 TIA V20。
- PLC 型号已确认：SIMATIC S7-1500 CPU 1515T-2 PN，IP 为 `192.168.1.1`。当前驱动正好按 S7-1500 连接，不需要改代码。
- 网关 `GateWay.exe`、MariaDB、测试脚本都在宿主机 `<REPO_ROOT>`，不需要装进虚拟机。
- 宿主机当前 Wi-Fi IP 是 `192.168.4.227`，自带网口（Realtek）现在没插网线。
- 数据库账号 `root/root`、库名 `scada` 已经配好。
- 当前 `3306/502/6543` 端口都没有监听，说明 MariaDB、模拟器、网关还没启动，第 7 步会启动。

整体连接图：

```text
PLC (192.168.1.1)
    | 网线直连
笔记本网口 (192.168.1.50)   <-- 宿主机：GateWay + MariaDB + CoreTest
    | VMware 桥接
虚拟机 Windows (192.168.1.60)   <-- TIA V20，用来给 PLC 编程、改值
```

## 1. 先确认 6 件事

1. PLC 具体型号：已确认是 `SIMATIC S7-1500 CPU 1515T-2 PN`，驱动默认按 S7-1500 连接，正好匹配。以后如果换 S7-1200/300/400，需要先改驱动。
2. PLC 有没有以太网口：S7-1200/1500 大多自带 RJ45 网口；S7-300/400 可能要 CP 通讯模块。
3. PLC 现在的 IP：已确认是 `192.168.1.1`。如果实际和这个不一致，第 5 步用 TIA 在线搜索并改。
4. 一根能用的网线。
5. TIA V20 里有没有现成项目：有就直接用，没有就新建。
6. PLC 接哪个口：推荐直接插笔记本网口。如果只能走公司交换机或路由器，就按那个网段重新规划 IP。

## 2. 规划 IP（推荐方案）

| 设备 | IP 地址 | 子网掩码 |
| --- | --- | --- |
| PLC | `192.168.1.1` | `255.255.255.0` |
| 笔记本网口 | `192.168.1.50` | `255.255.255.0` |
| 虚拟机 Windows | `192.168.1.60` | `255.255.255.0` |


## 3. 给笔记本网口设固定 IP（宿主机）

1. 打开“设置 -> 网络和 Internet -> 以太网”。
2. 点“IP 分配”右侧的“编辑”，选“手动”，打开 IPv4。
3. 填 `192.168.1.50`，子网掩码 `255.255.255.0`，网关和 DNS 留空，保存。
4. 用网线把 PLC 和笔记本网口连起来。右下角网口图标变成“已连接”（显示“未识别的网络”也没关系）。
5. 验证：在 PowerShell 里执行 `ipconfig`，看到“以太网”下面 IPv4 是 `192.168.1.50` 即可。

## 4. 配置 VMware 虚拟机网络（桥接）

目标：让虚拟机里的 TIA 能直接访问 PLC。

1. 先关闭虚拟机（改网卡设置前关机更稳）。
2. VMware Workstation 里：右键虚拟机 -> 设置 -> 网络适配器：
   - 选“桥接模式：直接连接物理网络”，勾选“打开电源时连接”。
3. 菜单“编辑 -> 虚拟网络编辑器”（需要管理员权限）：
   - 选 `VMnet0`，类型选“桥接”，“桥接到”选物理网卡 `Realtek PCIe GbE Family Controller`（中文界面可能显示“以太网”）。
   - 不指定这一项时，VMware 可能把桥接指到 Wi-Fi，导致 TIA 搜不到直连的 PLC。
4. 启动虚拟机，进入 Windows，把虚拟机里的网卡也设成静态 IP：`192.168.1.60` / `255.255.255.0`。
5. 在虚拟机里验证：
   ```powershell
   ping 192.168.1.50   # 宿主机
   ping 192.168.1.1    # PLC，前提是 PLC 已上电且 IP 已是 192.168.1.1
   ```
   - ping 不通时，先临时关闭虚拟机里的 Windows 防火墙再试。
   - 最终标准：TIA 的“在线访问 -> 更新可访问的设备”能搜到 PLC。

## 5. 在 TIA V20 里把 PLC 准备好（虚拟机里操作）

### 5.1 项目和设备

新建项目（或打开现有项目）-> 添加新设备 -> 选择与实物一致的型号 `CPU 1515T-2 PN`。

### 5.2 给 PLC 设 IP

“设备组态 -> PROFINET 接口 -> 以太网地址”，IP 设为 `192.168.1.1`，子网掩码 `255.255.255.0`。

如果实际 IP 和 `192.168.1.1` 不一致：用“在线访问”搜索设备，进“在线和诊断 -> 分配 IP 地址”，把 PLC 改成 `192.168.1.1`。

### 5.3 开放 S7 通信（PUT/GET）

- S7-1200/1500：设备组态 -> 防护与安全 -> 连接机制 -> 勾选“允许来自远程伙伴的 PUT/GET 通信访问”。
- S7-300/400：需要允许 PG/OP 通信，做法不一样。如果是 300/400，先告诉我型号，我再给你对应步骤。

### 5.4 新建 DB1（必须是非优化访问）

1. “程序块 -> 添加新块 -> 数据块（全局）”，名称写 `DB1`。
2. 在 DB 属性里取消勾选“优化的块访问”，否则 S7.Net 无法按地址读取，网关会一直读不到值。
3. 在 DB1 里按表添加变量，地址不用手打，编译后自动显示：

| 变量名 | 类型 | 编译后偏移 | 对应网关标签 |
| --- | --- | --- | --- |
| `bTest` | Bool | `DBX0.0` | 1001 `S7_Test_Bool0` |
| `iTest1` | Int | `DBW2` | 1002 `S7_Test_Int2` |
| `iTest2` | Int | `DBW4` | 1003 `S7_Test_Int4` |
| `rTest` | Real | `DBD8` | 1004 `S7_Test_Real8` |
| `dTest` | DInt | `DBD12` | 1005 `S7_Test_DInt12` |

> 实测这台 PLC 的 DB1 只有 12 字节（0..11），里面没有 `DBD12` 的 `dTest`，所以标签 1005 默认已停用。
> 以后在 TIA 里给 DB1 补一个 `DInt`（地址会落在 `DBD12`）并下载后，再执行
> `UPDATE scada.meta_tag SET IsActive=b'1' WHERE TagID=1005;` 并重启网关即可启用。

### 5.5 建 M0.0

`M0.0` 不在 DB1 里。在“PLC 变量表”里加一个 Bool 变量 `mTest`，地址 `M0.0`，对应网关标签 1006。

### 5.6 编译并下载

1. 保存并编译整个项目，确认没有错误。
2. “下载到设备”：通讯接口选虚拟机里那块网卡，点“开始搜索”，找到 IP 为 `192.168.1.1` 的 PLC，把硬件配置和程序一起下载。
3. 下载完把 PLC 切到 `RUN`（运行）。

### 5.7 产生测试值

打开监视表或 DB1 在线监视，把 `iTest1` 改成 `1234`，`rTest` 改成 `3.14`，再切换 `bTest`、`mTest`。这些变化稍后要在宿主机 CoreTest 里看到。

## 6. 改数据库里的 PLC IP（宿主机）

网关从数据库 `meta_driver` 表读 PLC 地址。你确认的 PLC IP 是 `192.168.1.1`，Codex 已把它写进数据库，可以先用下面的 `SELECT` 确认。以后 PLC 换 IP 时，用下面命令修改：

```powershell
powershell -ExecutionPolicy Bypass -File <REPO_ROOT>\outputs\config\set_s7_ip.ps1 192.168.1.1
```

把命令最后的 IP 换成真实 PLC IP。脚本会先执行 `UPDATE`，再 `SELECT` 给你确认。

想手动确认也可以：

```powershell
& '<REPO_ROOT>\work\mariadb\mariadb-11.4.2-winx64\bin\mysql.exe' --skip-ssl --host=127.0.0.1 --port=3306 -uroot -proot --execute="SELECT DriverID, DriverName, Server FROM scada.meta_driver WHERE DriverID=2;"
```

注意：改完 IP 后必须重启网关，网关只在启动时连接 PLC。

## 7. 启动服务并验证（宿主机）

1. 一键启动 MariaDB、Modbus 模拟器和网关（已在监听的会自动跳过）：
   ```powershell
   powershell -ExecutionPolicy Bypass -File <REPO_ROOT>\work\restart_all.ps1
   ```
2. 确认三个端口都在监听：
   ```powershell
   netstat -ano | Select-String ':3306|:502|:6543'
   ```
3. 读 S7 标签（多执行几次，看值是否变化）：
   ```powershell
   python <REPO_ROOT>\outputs\probes\query_tag.py 1002 2
   python <REPO_ROOT>\outputs\probes\query_tag.py 1004 4
   python <REPO_ROOT>\outputs\probes\query_tag.py 1006 1
   ```
   `1002` 应显示 `d204`（1234），`1004` 应显示 `c3f54840`（3.14）。原始字节是小端显示，不需要人工换算；
   1005 因 PLC 里没有 `DBD12` 默认停用，不会出现在读列表里。
4. 打开客户端：
   ```powershell
   <REPO_ROOT>\work\SharpSCADA\SCADA\Example\CoreTest.exe
   ```
   在实时标签列表里找 `S7_Test_*`。回 TIA 改 `iTest1`/`rTest`，客户端 1 秒内应跟着变。

### 7.5 归档落库验证（已开启，可复核）

1001-1004、1006 已开启归档：`Archive=1`、`Cycle=1000`（每 1000ms 采样一次），已写进
`outputs\config\s7_config.sql`，重复执行配置脚本不会把归档关掉。先确认数据库状态：

```powershell
& '<REPO_ROOT>\work\mariadb\mariadb-11.4.2-winx64\bin\mysql.exe' --skip-ssl --host=127.0.0.1 --port=3306 -uroot -proot --database=scada --execute="SELECT TagID, Archive+0, Cycle FROM meta_tag WHERE TagID IN (1001,1002,1003,1004,1006);"
```

网关只在启动时读 `meta_tag`，改完归档配置必须重启：

```powershell
Get-Process GateWay -ErrorAction SilentlyContinue | Stop-Process -Force
$gw = '<REPO_ROOT>\work\SharpSCADA\SCADA\Program\CoreApp\DataService\GateWay\bin\Debug\net10.0\GateWay.exe'
Start-Process -FilePath $gw -WorkingDirectory (Split-Path $gw) -WindowStyle Hidden
```

批量落库由 `C:\DataConfig\server.xml` 的 `<Hda>` 配置控制（工作区副本在
`<REPO_ROOT>\work\SharpSCADA\SCADA\DataConfig\server.xml`，两处要同步）。
默认 `MaxHdaCap=10000`、`WriteCycle=600000`、`Delay=3600000`，不会立刻落库；快速验证时
临时把 `WriteCycle` 和 `Delay` 都调成 `30000` 并重启网关，约 30 秒后查询：

```powershell
& '<REPO_ROOT>\work\mariadb\mariadb-11.4.2-winx64\bin\mysql.exe' --skip-ssl --host=127.0.0.1 --port=3306 -uroot -proot --database=scada --execute="SELECT ID, TIMESTAMP, VALUE FROM log_hdata WHERE ID IN (1001,1002,1003,1004,1006) ORDER BY TIMESTAMP DESC LIMIT 20;"
```

能看到 1001-1004、1006 的行即验证通过。验证完把两个值调回 `600000`/`3600000`
（`WriteCycle` 和 `Delay`），再重启网关恢复默认批量落库节奏。

## 8. 常见问题对照表

| 现象 | 检查 |
| --- | --- |
| 宿主机 ping 不通 PLC | 网线、PLC 是否上电、网口 IP 是否同网段、PLC IP 是否设对 |
| TIA 搜不到 PLC | 虚拟机是否桥接到“以太网”物理网卡、虚拟机 IP 是否 `192.168.1.60`、防火墙 |
| TCP 102 连不上 | TIA 里没勾 PUT/GET、PLC 没下载或没运行、网段不对 |
| DB 读不到值 | DB1 是“优化的块访问”，改成非优化后重新下载 |
| 标签一直 0/不更新 | 数据库 Server 没改、网关没重启、标签地址和类型与 DB 不一致、查 `<REPO_ROOT>\work\gw_debug.log` |
| DB 读整块报“Address out of range” | 标签里的地址超出 DB 实际长度（例如 DB1 只有 0..11 却配置了 DBD12），停用多余标签或改对地址后重启网关；详情见 `<REPO_ROOT>\work\s7read_errors.log` |
| 改了 IP 还是连旧地址 | 网关只在启动时连接，改完配置必须重启网关 |
| 以后换非 S7-1500 型号 | 需要改 `S7TCPReader.cs` 第 117 行 CPU 类型并重新编译；当前 `1515T-2 PN` 不需要 |
